spring.application.name=comic-search
# Application properties

# Server port (optional, defaults to 8080)
server.port=8080

# Feign client configuration for logging (optional, useful for debugging API calls)
logging.level.feign.client=DEBUG
